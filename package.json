{
  "private": true,
  "devDependencies": {
    "vscode-tmgrammar-test": "^0.0.8"
  },
  "scripts": {
    "test": "yarn run test:snap",
    "test:snap": "vscode-tmgrammar-snap -g OCaml.plist -s source.ocaml -t 'test/snap/*.ml'"
  }
}
